# Built and publishable, regenerable from _source/ — the seeded canvas
# carries a ~3 MB compiled editor, so neither belongs in the repository.
planning-imagerie-cardiaque.html
prototype-planning-imagerie.html
